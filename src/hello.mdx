import {
  // Accordion,
  Button,
  Item,
  Provider,
  View,
  darkTheme,
  defaultTheme,
  lightTheme,
} from '@adobe/react-spectrum';
import { Accordion } from '@react-spectrum/accordion';

# hello.mdx

## usage

This is an example using webpack

<Button variant='cta'> This button in hello.mdx // WORKS WELL</Button>

- if Accordion is commented, rendering works.
- if Accordion is not commented, build fails.

{/* <Accordion>
  <Item key='files' title='This Accordion in hello.mdx'>
    files
  </Item>
</Accordion> */}
